pipeline {
  agent any
    stages {
    stage ('SCM checkout') {
      steps {
        sh 'echo "git checkout"'
      }
    }
    stage ('Build') {
      steps {
        sh 'echo "mvn clean install"'
      }
    }
    stage ('Deploy') {
      steps {
        sh 'echo "Deploy to the tomcat server"'
      }
    }
  }
}
